[project.scripts]
patentx = "patentx.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "patentx"
version = "0.1.0"
description = "Redaction-safe sample: patent PDF extraction architecture (public interfaces)."
readme = "README.md"
requires-python = ">=3.9"
authors = [{name="Your Name"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]
